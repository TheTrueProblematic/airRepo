Global Aircraft Registry Aggregator & Automated Test SuiteWelcome to the documentation guide for the Global Aircraft Registry Aggregator. This document serves as a operational reference manual for developers, systems engineers, and agentic AI models. It describes the design requirements, the system architecture of our implementation, and instructions on how to set up, run, and troubleshoot the application.1. System Requirements & Goals (What Was Requested)The primary goal of this project is to build a unified aircraft registry search engine capable of identifying and extracting information about an aircraft using only its tail number (registration mark).Key Objectives:Interactive UI with Headless Fallbacks: Provide a user-friendly graphical popup dialog for manual search inputs, with automatic fallbacks to command-line input or arguments on headless servers (Linux, macOS, Windows).Strict Tail Number Normalization: Clean the user input to strip spaces/hyphens and identify the registering nation's civil aviation authority (CAA) prefix.Example: If searching C-GMJF or CGMJF (Canada), isolate the unique search key GMJF and direct queries to the Canadian registry.Example: If searching a raw numeric value like 737AA (United States), assume the leading prefix N is omitted and format as N737AA.Database-First Caching Strategy: To avoid hammering external government servers, search requests must query a local cache database first.On-Demand Registries Ingestion: On cache misses, automatically download, extract, and index the entire bulk data archives of supported nations:FAA (United States): Relational .txt parsing (relates MASTER.txt to ACFTREF.txt).CCAR (Canada): Parses and joins carscurr.txt (aircraft) and carsownr.txt (owners).CASA (Australia): Unpacks weekly compressed CSVs.CAA (New Zealand): Parses flat-file monthly CSVs.ANAC (Brazil): Indexes semi-colon delimited ISO-8859-1 CSV registries.IAA (Ireland): Parses monthly .xlsx Excel spreadsheets directly without external library dependencies.Live Scraping for Restricted Registries (United Kingdom): Because the UK CAA does not offer open bulk downloads, dynamically spawn a headless Selenium automation browser to run live search queries against the G-INFO portal and cache successful results in SQLite.Graceful Fallbacks & Local Mocking: If an external server goes offline, gracefully catch the error and load verified, test-compliant offline mock data for that region to preserve run-time search functionality.Comprehensive Diagnostics: Output verbose, self-clearing execution files (log.log and test.log) detailing database hits, parse performance, and connection states.2. Implemented Architecture (What Was Built)The system consists of two primary operational Python scripts cooperating with an embedded relational database:├── airRepo.py            # Primary Engine (Main CLI, GUI, Parsers, & Scrapers)
├── test.py              # End-to-End Automated Testing Suite (Run by Humans or AI Agents)
├── aircraft_registry.db # Unified SQLite database (Autogenerated on first run)
├── log.log              # Real-time application event logs (Self-cleans on run)
└── test.log             # Dedicated test suite diagnostic trace logs (Self-cleans on run)
The Database Structure (aircraft_registry.db)Our database implements a flat, optimized, indexable table layout under SQLite:Column NameSQLite DatatypeDescriptiontailTEXT PRIMARY KEYStandardized index key (e.g., GMJF, N91GF, VHOJA)original_tailTEXTStandardized presentation-ready visual string (e.g., C-GMJF, VH-OJA)regionTEXTCountry of Registration (e.g., Canada, United States, Australia)make_modelTEXTNormalized aircraft manufacturer & model metadatadate_manufacturedTEXTCalendar year of construction/deliveryownerTEXTRegistered legal corporate entity or physical individual name3. Environment & PrerequisitesThis software is designed to run seamlessly across macOS, Windows, and Linux.Python EnvironmentRequires Python 3.8+.Ensure standard libraries (sqlite3, urllib, zipfile, xml) are installed as part of your core distribution.Optional External Dependencies (Selenium Setup)To scrape the UK G-INFO portal live, you need Selenium and a local installation of the Google Chrome browser:# Install Python Selenium bindings
pip install selenium
Note: The Selenium driver uses Chrome's headless mode (--headless=new), meaning no physical browser window will pop up during dynamic scraping.4. How to Run the SystemInteractive Search Mode (User Facing)Double-click airRepo.py or run it from your terminal:python airRepo.py
If Display Head is Active: A native Tkinter GUI dialogue box will prompt you for the tail number.If Display Head is Headless (SSH, Linux Shell): The program will gracefully fall back to a terminal-input prompt: Enter Aircraft Tail Number: .Command-Line Arguments Mode (CLI Integration)Directly pass the tail number as an inline terminal argument to bypass dialogue menus entirely:python airRepo.py N737AA
5. Automated Integration Testing (test.py)The automated suite tests multiple registry variations, normalization paths, cache policies, and unsupported classification errors without launching GUI blockers.Running the Test SuiteFrom the system console, invoke test.py:python test.py
What the Test Suite Evaluates:The script maps input queries to expected behaviors through a predefined test matrix:TEST_MATRIX = [
    # US Tail Variations
    ("N91GF", "United States", False, "US Prefix"),
    ("737AA", "United States", False, "US Numeric/Missing N"),
    ("N12345", "United States", False, "US Standard"),
    
    # Canada Tail Variations
    ("C-GMJF", "Canada", False, "Canada Dash"),
    ("CFZRR", "Canada", False, "Canada Non-Dash Standard"),
    ("CGMJF", "Canada", False, "Canada Short Non-Dash"),
    
    # UK Tail Variations
    ("G-INFO", "United Kingdom", False, "UK Dash Standard"),
    ("GINFO", "United Kingdom", False, "UK Non-Dash"),
    
    # Other Supported Registries
    ("VH-OJA", "Australia", False, "Australia Dash"),
    ("ZK-PQR", "New Zealand", False, "New Zealand Dash"),
    ("PP-XYZ", "Brazil", False, "Brazil Standard"),
    ("EI-LBS", "Ireland", False, "Ireland Excel Ingestion"),
    
    # Unsupported Registries
    ("D-ATRA", "Germany", True, "Unsupported Region (Germany)"),
    ("B-1234", "China / Taiwan", True, "Unsupported Region (China)")
]
Understanding Test OutputWhen running, the test suite generates a clean terminal table summarizing performance:=====================================================================================
                             TEST RUN SUMMARY REPORT
=====================================================================================
INPUT TAIL   | CATEGORY                     | STATUS | RESULT DETAILS / ERRORS
-------------------------------------------------------------------------------------
N91GF        | US Prefix                    | PASS   | CESSNA 172S | SKYHAWK LEASING LLC
737AA        | US Numeric/Missing N         | PASS   | BOEING 737-800 | AMERICAN AIRL...
C-GMJF       | Canada Dash                  | PASS   | CHALLENGER 604 | BOMBARDIER INC
D-ATRA       | Unsupported Region (Germany) | PASS   | Gracefully Blocked / Not Su...
=====================================================================================
6. Diagnostic Logging & TroubleshootingTwo detailed log files are generated in the application folder:log.log (Engine Logs): Captures SQL transaction entries, URL download progress metrics, XML parser execution traces, and Selenium driver states.test.log (Test Suite Logs): Traces test harness initialization, pre-flight library audits, and test case evaluation metadata.Frequently Asked Issues"Missing import module" on run: Ensure your main aggregator script is named either airRepo.py or aircraft_lookup.py. test.py scans for both names dynamically before aborting.Selenium Scraper fails / falls back: If Google Chrome is missing or Selenium is uninstalled, airRepo.py will catch the error, write standard trace diagnostics to log.log, and gracefully pull fallback mock data into your local database so the test run passes successfully.

7. Agent Mandatory Guidelines (Critical Verification & Deployment)
All agentic AI models, automated CI runners, and developers MUST strictly adhere to the following workflow before returning or completing any task:

A. PRE-FLIGHT SYSTEM TESTING
You must verify the integrity of the database normalization, caching, parsing, and scraping algorithms by executing the automated test suite locally inside the `API` directory:
```bash
cd API
python3 test.py
```
This test suite performs strict assertions on key registry tails (such as `C-FZRR` verifying Cessna 206H registered to Sky Photo Techniques, and `N91GF` verifying Cessna 172S). 
- Every single test case MUST output a green `PASS`.
- If any test fails, do NOT push to cloud or finalize the task. Fix the codebase first.

B. DATABASE RE-SEEDING (IF SCHEMA OR DATA WAS MODIFIED)
If you made changes affecting database schemas, ingestion logic, or mock fallback registry data, you must re-seed the databases:
1. Re-seed local SQLite database for local test runs:
   ```bash
   python3 seed.py
   ```
2. Re-seed the Firestore production instance (if environment demands it):
   ```bash
   export AIRREPO_BACKEND=firestore
   export GOOGLE_CLOUD_PROJECT=airrepo-8e96a
   python3 seed.py
   ```

C. FIREBASE & GOOGLE CLOUD DEPLOYMENTS
Once and ONLY once all tests pass, you must deploy the updates:
1. Deploy Backend API to Google Cloud Run:
   ```bash
   cd API
   gcloud builds submit --tag us-central1-docker.pkg.dev/airrepo-8e96a/airrepo/api:latest .
   gcloud run deploy airrepo-api \
     --image us-central1-docker.pkg.dev/airrepo-8e96a/airrepo/api:latest \
     --region us-central1 \
     --platform managed \
     --allow-unauthenticated \
     --set-env-vars "GOOGLE_CLOUD_PROJECT=airrepo-8e96a"
   ```
2. Deploy React Frontend to Firebase Hosting:
   ```bash
   cd ../Web
   npm run build
   firebase deploy --only hosting
   ```